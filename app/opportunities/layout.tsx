import {routeMetadata} from "@/lib/seo";
export const metadata=routeMetadata("Opportunity Radar","Find crypto assets showing the strongest changes in SparkScore and regime behaviour.","/opportunities");
export default function Layout({children}:{children:React.ReactNode}){return children;}
