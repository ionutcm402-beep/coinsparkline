import {routeMetadata} from "@/lib/seo";
export const metadata=routeMetadata("Crypto Screener","Filter crypto assets by SparkScore, regime state and market conditions to find assets matching your research criteria.","/screener");
export default function Layout({children}:{children:React.ReactNode}){return children;}
