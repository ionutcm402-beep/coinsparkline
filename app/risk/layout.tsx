import {routeMetadata} from "@/lib/seo";
export const metadata=routeMetadata("Risk","Understand the limitations, uncertainty and risk around crypto market signals and model interpretation.","/risk");
export default function Layout({children}:{children:React.ReactNode}){return children;}
