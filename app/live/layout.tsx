import {routeMetadata} from "@/lib/seo";
export const metadata=routeMetadata("CoinSpark Live","Real-time crypto market context and community conversation with live market state and ticker-linked discussion.","/live");
export default function Layout({children}:{children:React.ReactNode}){return children;}
