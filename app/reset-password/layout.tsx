import {routeMetadata} from "@/lib/seo";
export const metadata=routeMetadata("Reset password","Choose a new password for your CoinSparkLine account.","/reset-password",{noIndex:true});
export default function Layout({children}:{children:React.ReactNode}){return children;}
