import {routeMetadata} from "@/lib/seo";
export const metadata=routeMetadata("Forgot password","Recover access to your CoinSparkLine account.","/forgot-password",{noIndex:true});
export default function Layout({children}:{children:React.ReactNode}){return children;}
