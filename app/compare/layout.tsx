import {routeMetadata} from "@/lib/seo";
export const metadata=routeMetadata("Compare crypto behaviour","Compare SparkScore, regime state and historical context across crypto assets.","/compare");
export default function Layout({children}:{children:React.ReactNode}){return children;}
