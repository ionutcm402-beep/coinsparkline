import {routeMetadata} from "@/lib/seo";
export const metadata=routeMetadata("Crypto School","Learn crypto market behaviour, regimes, risk and CoinSparkLine research concepts in plain language.","/school");
export default function Layout({children}:{children:React.ReactNode}){return children;}
