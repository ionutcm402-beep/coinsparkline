import type{MetadataRoute}from"next";import{getLatestScan}from"@/lib/blobStorage";
export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://coinsparkline.com",now=new Date();const staticPaths=["","/radar","/explore","/about","/methodology","/data-sources","/status","/school","/risk","/disclaimer","/privacy","/cookies","/terms"];const snapshot=await getLatestScan();return[...staticPaths.map(path=>({url:`${base}${path}`,lastModified:now,changeFrequency:path===""||path==="/radar"?"daily" as const:"weekly" as const,priority:path===""?1:path==="/radar"?.9:.6})),...(snapshot?.coins??[]).map(c=>({url:`${base}/coin/${c.id}`,lastModified:snapshot?.scannedAt?new Date(snapshot.scannedAt):now,changeFrequency:"daily" as const,priority:.8}))]}
