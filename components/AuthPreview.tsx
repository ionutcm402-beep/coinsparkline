"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";
export default function AuthPreview({mode}:{mode:"signin"|"signup"}){const[notice,setNotice]=useState(false);function submit(e:FormEvent){e.preventDefault();setNotice(true)}const signup=mode==="signup";return <main className="mx-auto flex min-h-[65vh] max-w-[460px] items-center px-4 py-10"><div className="w-full rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_14px_45px_rgba(20,35,75,.07)]"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-slate-400">CoinSparkLine account</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em] text-slate-950">{signup?"Create your account":"Welcome back"}</h1><p className="mt-2 text-sm leading-6 text-slate-500">{signup?"Prepare for synced watchlists, portfolio and alerts across devices.":"Sign in will sync your CoinSparkLine tools once account authentication is connected."}</p><form onSubmit={submit} className="mt-6 space-y-3">{signup&&<input required placeholder="Name" className="w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm"/>}<input required type="email" placeholder="Email" className="w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm"/><input required type="password" placeholder="Password" className="w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm"/><button className="w-full rounded-lg bg-gradient-to-r from-blue-600 to-violet-600 px-4 py-2.5 text-sm font-semibold text-white">{signup?"Sign up":"Sign in"}</button></form>{notice&&<div className="mt-3 rounded-lg bg-amber-50 px-3 py-2 text-xs leading-5 text-amber-800">The account interface is ready, but authentication is not connected yet. No credentials were stored or submitted.</div>}<p className="mt-5 text-center text-xs text-slate-500">{signup?"Already have an account? ":"New to CoinSparkLine? "}<Link href={signup?"/signin":"/signup"} className="font-semibold text-blue-600">{signup?"Sign in":"Sign up"}</Link></p></div></main>}
