"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";
import {getSupabaseBrowserClient} from "@/lib/supabaseClient";

export default function AuthPreview({mode}:{mode:"signin"|"signup"}){
 const signup=mode==="signup";
 const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");
 const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);
 async function submit(e:FormEvent){e.preventDefault();setBusy(true);setMessage("");try{const supabase=getSupabaseBrowserClient();if(signup){const{error}=await supabase.auth.signUp({email,password,options:{data:{name}}});if(error)throw error;setMessage("Account created. Check your email if confirmation is enabled, then sign in.");}else{const{error}=await supabase.auth.signInWithPassword({email,password});if(error)throw error;setMessage("Signed in successfully. Your watchlist and alert rules can now sync to your account.");setTimeout(()=>{window.location.href="/"},700);}}catch(err){setMessage(err instanceof Error?err.message:"Authentication failed");}finally{setBusy(false)}}
 return <main className="mx-auto flex min-h-[65vh] max-w-[460px] items-center px-4 py-10"><div className="w-full rounded-2xl border border-slate-200 bg-white p-6 shadow-[0_14px_45px_rgba(20,35,75,.07)]"><p className="text-[10px] font-bold uppercase tracking-[.12em] text-slate-400">CoinSparkLine account</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.04em] text-slate-950">{signup?"Create your account":"Welcome back"}</h1><p className="mt-2 text-sm leading-6 text-slate-500">{signup?"Create an account to sync alerts and CoinSparkLine tools across devices.":"Sign in to access synced alerts and your CoinSparkLine account."}</p><form onSubmit={submit} className="mt-6 space-y-3">{signup&&<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Name" className="w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm"/>}<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm"/><input required minLength={6} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm"/><button disabled={busy} className="w-full rounded-lg bg-gradient-to-r from-blue-600 to-violet-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{busy?"Please wait…":signup?"Sign up":"Sign in"}</button></form>{message&&<div className="mt-3 rounded-lg bg-slate-50 px-3 py-2 text-xs leading-5 text-slate-700">{message}</div>}<p className="mt-5 text-center text-xs text-slate-500">{signup?"Already have an account? ":"New to CoinSparkLine? "}<Link href={signup?"/signin":"/signup"} className="font-semibold text-blue-600">{signup?"Sign in":"Sign up"}</Link></p></div></main>
}
