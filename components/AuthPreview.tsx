"use client";
import Link from "next/link";
import {FormEvent,useId,useState} from "react";
import {useSearchParams} from "next/navigation";
import {getSupabaseBrowserClient} from "@/lib/supabaseClient";
export default function AuthPreview({mode}:{mode:"signin"|"signup"}){const signup=mode==="signup";const params=useSearchParams();const requested=params.get("next")||"/signals";const next=requested.startsWith("/")&&!requested.startsWith("//")?requested:"/signals";const uid=useId();const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[showPassword,setShowPassword]=useState(false);const[message,setMessage]=useState("");const[isError,setIsError]=useState(false);const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(busy)return;setBusy(true);setMessage("");setIsError(false);try{const supabase=getSupabaseBrowserClient();if(signup){const{data,error}=await supabase.auth.signUp({email:email.trim(),password,options:{data:{name:name.trim()}}});if(error)throw error;if(data.session){setMessage("Account created. Opening My Signals…");window.setTimeout(()=>{window.location.href=next},500)}else setMessage("Account created. Check your email to confirm it, then sign in and your signals will sync.");}else{const{error}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(error)throw error;setMessage("Signed in. Opening My Signals…");window.setTimeout(()=>{window.location.href=next},350)}}catch(err){setIsError(true);setMessage(err instanceof Error?err.message:"Authentication failed");}finally{setBusy(false)}}return <main className="csl-auth-shell"><section className="csl-auth-card" aria-labelledby={`${uid}-title`}><p className="csl-trust-eyebrow">CoinSparkLine account</p><h1 id={`${uid}-title`}>{signup?"Keep your signals with you.":"Welcome back"}</h1><p>{signup?"Sync the markets you watch, portfolio records and alert conditions across devices.":"Sign in to return directly to My Signals and your synced market conditions."}</p><form onSubmit={submit} className="csl-auth-form" aria-describedby={message?`${uid}-status`:undefined}>{signup&&<label className="csl-auth-label">Name<input required autoComplete="name" value={name} onChange={e=>setName(e.target.value)} className="csl-auth-field"/></label>}<label className="csl-auth-label">Email<input required autoComplete="email" inputMode="email" type="email" value={email} onChange={e=>setEmail(e.target.value)} className="csl-auth-field"/></label><label className="csl-auth-label">Password<span className="relative block"><input required minLength={6} autoComplete={signup?"new-password":"current-password"} type={showPassword?"text":"password"} value={password} onChange={e=>setPassword(e.target.value)} className="csl-auth-field pr-16"/><button type="button" onClick={()=>setShowPassword(v=>!v)} className="absolute right-2 top-1/2 -translate-y-1/2 rounded-lg px-2.5 py-2 text-[10px] font-semibold text-slate-500" aria-pressed={showPassword}>{showPassword?"Hide":"Show"}<span className="sr-only"> password</span></button></span></label><button disabled={busy} className="csl-btn-primary h-12 w-full justify-center disabled:cursor-wait disabled:opacity-55">{busy?"Please wait…":signup?"Create account":"Sign in"}</button></form>{message&&<div id={`${uid}-status`} role={isError?"alert":"status"} aria-live={isError?"assertive":"polite"} className="csl-auth-status">{message}</div>}<p className="mt-5 text-center">{signup?"Already have an account? ":"New to CoinSparkLine? "}<Link href={`${signup?"/signin":"/signup"}?next=${encodeURIComponent(next)}`} className="font-semibold text-blue-600">{signup?"Sign in":"Sign up"}</Link></p><p className="mt-4 text-center text-[11px] text-slate-400">Accounts enable syncing and alerts; they do not turn CoinSparkLine signals into investment advice.</p></section></main>}
