"use client";
import Link from "next/link";
import {FormEvent,useState} from "react";
import {getSupabaseBrowserClient} from "@/lib/supabaseClient";
import Clean2030Controls from "@/components/Clean2030Controls";

export default function ResetPassword2030(){const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[busy,setBusy]=useState(false);const[message,setMessage]=useState("");const[error,setError]=useState(false);async function submit(e:FormEvent){e.preventDefault();if(busy)return;if(password.length<6||password!==confirm){setError(true);setMessage(password!==confirm?"Passwords do not match.":"Password must be at least 6 characters.");return}setBusy(true);setMessage("");setError(false);try{const{error}=await getSupabaseBrowserClient().auth.updateUser({password});if(error)throw error;setMessage("Password updated. You can now sign in with your new password.")}catch(err){setError(true);setMessage(err instanceof Error?err.message:"Unable to update password")}finally{setBusy(false)}}return <div className="clean2030 clean2030-auth-page"><header className="clean2030-top"><Link href="/crypto" className="clean2030-brand">Coin<span>SparkLine</span></Link><nav className="clean2030-nav"><Link href="/crypto">CRYPTO</Link><Link href="/nft">NFT</Link><Link href="/portfolio">PORTFOLIO</Link></nav><Clean2030Controls/></header><main className="clean2030-auth-wrap"><section className="clean2030-auth-card"><span className="clean2030-kicker">Account recovery</span><h1>Choose a new password.</h1><p>Use a new password for your CoinSparkLine account.</p><form onSubmit={submit}><label>New password<input required minLength={6} type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label>Confirm password<input required minLength={6} type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><button disabled={busy}>{busy?"UPDATING…":"UPDATE PASSWORD"}</button></form>{message?<div className="clean2030-auth-message" data-error={error}>{message}</div>:null}<p className="clean2030-auth-switch"><Link href="/signin">Go to sign in</Link></p></section></main></div>}
