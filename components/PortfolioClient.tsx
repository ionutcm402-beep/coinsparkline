"use client";
import {FormEvent,useEffect,useMemo,useState} from "react";

type Holding={id:string;symbol:string;name:string;quantity:number;averagePrice:number};
const KEY="csl-portfolio-v1";
export default function PortfolioClient(){
 const[holdings,setHoldings]=useState<Holding[]>([]);const[symbol,setSymbol]=useState("");const[name,setName]=useState("");const[quantity,setQuantity]=useState("");const[price,setPrice]=useState("");
 useEffect(()=>{try{setHoldings(JSON.parse(localStorage.getItem(KEY)||"[]"))}catch{setHoldings([])}},[]);
 function save(next:Holding[]){setHoldings(next);localStorage.setItem(KEY,JSON.stringify(next))}
 function add(e:FormEvent){e.preventDefault();const q=Number(quantity),p=Number(price);if(!symbol.trim()||!Number.isFinite(q)||q<=0||!Number.isFinite(p)||p<0)return;save([...holdings,{id:crypto.randomUUID(),symbol:symbol.trim().toUpperCase(),name:name.trim()||symbol.trim().toUpperCase(),quantity:q,averagePrice:p}]);setSymbol("");setName("");setQuantity("");setPrice("")}
 const invested=useMemo(()=>holdings.reduce((s,h)=>s+h.quantity*h.averagePrice,0),[holdings]);
 return <div className="mx-auto max-w-[1050px] px-4 py-8 sm:px-5"><div className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[.12em] text-slate-400">Personal dashboard</p><h1 className="mt-1 text-3xl font-semibold tracking-[-.04em] text-slate-950">Portfolio</h1><p className="mt-2 text-sm text-slate-500">A private local portfolio tracker. Holdings stay in this browser for now.</p></div><div className="rounded-xl border border-slate-200 bg-white px-5 py-3 text-right"><p className="text-[9px] font-bold uppercase tracking-wider text-slate-400">Cost basis</p><p className="mt-1 text-xl font-bold text-slate-950">${invested.toLocaleString(undefined,{maximumFractionDigits:2})}</p></div></div>
 <form onSubmit={add} className="mt-6 grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:grid-cols-2 lg:grid-cols-5"><input value={symbol} onChange={e=>setSymbol(e.target.value)} placeholder="Ticker, e.g. ZEC" className="rounded-lg border border-slate-200 px-3 py-2 text-sm"/><input value={name} onChange={e=>setName(e.target.value)} placeholder="Coin name" className="rounded-lg border border-slate-200 px-3 py-2 text-sm"/><input value={quantity} onChange={e=>setQuantity(e.target.value)} inputMode="decimal" placeholder="Quantity" className="rounded-lg border border-slate-200 px-3 py-2 text-sm"/><input value={price} onChange={e=>setPrice(e.target.value)} inputMode="decimal" placeholder="Average buy price $" className="rounded-lg border border-slate-200 px-3 py-2 text-sm"/><button className="rounded-lg bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Add holding</button></form>
 <div className="mt-4 overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="grid grid-cols-[1.2fr_.7fr_.8fr_.8fr_auto] gap-2 border-b border-slate-100 px-4 py-3 text-[9px] font-bold uppercase tracking-wider text-slate-400"><span>Asset</span><span>Quantity</span><span>Avg price</span><span>Cost basis</span><span/></div>{holdings.length?holdings.map(h=><div key={h.id} className="grid grid-cols-[1.2fr_.7fr_.8fr_.8fr_auto] items-center gap-2 border-b border-slate-100 px-4 py-3 text-sm last:border-0"><div><strong>{h.symbol}</strong><span className="ml-2 text-xs text-slate-400">{h.name}</span></div><span>{h.quantity}</span><span>${h.averagePrice.toLocaleString()}</span><span className="font-semibold">${(h.quantity*h.averagePrice).toLocaleString(undefined,{maximumFractionDigits:2})}</span><button onClick={()=>save(holdings.filter(x=>x.id!==h.id))} className="text-xs font-semibold text-rose-500">Remove</button></div>):<p className="p-8 text-center text-sm text-slate-400">No holdings yet. Add your first asset above.</p>}</div>
 <p className="mt-3 text-[10px] leading-4 text-slate-400">This first version is manual and browser-local. Live portfolio valuation and account sync can be connected after authentication is added.</p></div>;
}
