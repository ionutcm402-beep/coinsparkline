"use client";import Link from"next/link";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";import{createClient}from"@/lib/supabase/client";export default function AuthForm({mode}:{mode:"signin"|"signup"}){const signup=mode==="signup",router=useRouter(),[busy,setBusy]=useState(false),[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMessage("");const form=new FormData(e.currentTarget),email=String(form.get("email")||""),password=String(form.get("password")||""),name=String(form.get("name")||"");const supabase=createClient();if(signup){const{error}=await supabase.auth.signUp({email,password,options:{data:{name},emailRedirectTo:`${location.origin}/auth/confirm`}});setMessage(error?error.message:"Account created. Check your email to confirm your address.");setBusy(false);return}const{error}=await supabase.auth.signInWithPassword({email,password});if(error){setMessage(error.message);setBusy(false);return}router.push("/account");router.refresh()}return <main className="mx-auto flex min-h-[65vh] max-w-[440px] items-center px-4 py-10"><div className="w-full rounded-[22px] border border-slate-200 bg-white p-6 shadow-[0_14px_45px_rgba(20,35,75,.07)]"><p className="text-[9px] font-bold uppercase tracking-[.14em] text-violet-600">CoinSparkLine account</p><h1 className="mt-2 text-3xl font-semibold tracking-[-.05em]">{signup?"Create your account":"Welcome back"}</h1><p className="mt-2 text-[11px] leading-5 text-slate-500">{signup?"Sync your Watchlist, Portfolio, Alerts and preferences across devices.":"Sign in to access your personal CoinSparkLine workspace."}</p><form onSubmit={submit} className="mt-5 space-y-2.5">{signup&&<input name="name" required placeholder="Name" className="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-violet-300"/>}<input name="email" required type="email" autoComplete="email" placeholder="Email" className="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-violet-300"/><input name="password" required minLength={8} type="password" autoComplete={signup?"new-password":"current-password"} placeholder="Password · minimum 8 characters" className="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm outline-none focus:border-violet-300"/><button disabled={busy} className="w-full rounded-xl bg-gradient-to-r from-blue-600 to-violet-600 px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50">{busy?"Please wait…":signup?"Create account":"Sign in"}</button></form>{message&&<p className="mt-3 rounded-xl bg-slate-50 px-3 py-2 text-[10px] leading-5 text-slate-600">{message}</p>}<p className="mt-5 text-center text-[10px] text-slate-500">{signup?"Already have an account? ":"New to CoinSparkLine? "}<Link href={signup?"/signin":"/signup"} className="font-semibold text-blue-600">{signup?"Sign in":"Sign up"}</Link></p></div></main>}
